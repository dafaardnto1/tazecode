# TAZECODE API (Cloudflare Worker) — set this to your deployed Worker URL,
# e.g. https://tazecode-api.<your-subdomain>.workers.dev
# Leave empty during local dev to auto-fallback to http://127.0.0.1:8787
VITE_API_URL=
